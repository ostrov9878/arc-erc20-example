{
  "name": "arc-erc20-example",
  "version": "0.1.0",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network arcTestnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  }
}